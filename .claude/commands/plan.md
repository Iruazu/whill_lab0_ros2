---
description: pm-orchestrator を明示的に起動し、ユーザー要件を構造化計画に分解する。引数として要件の概要を渡す。
argument-hint: <要件の概要>
---

`pm-orchestrator` エージェントを起動し、以下の要件に対する実装計画を策定してください。

要件:
$ARGUMENTS

計画は `pm-orchestrator` の system prompt で指定されているフォーマットに従い、必要なら `docs/plans/` 配下に保存してください。

不明確な点があれば最大 3 つまで質問を返し、それ以上は合理的な前提を明文化して計画を進めてください。
