# Session log — 2026-05-30

Phase B (FAST-LIO-SAM with loop closure) closing session: the
self-exclusion patch was ported into FAST-LIO-SAM, the buried-map
symptom was tracked back to a non-standard IMU sign convention on the
PCMK-G3X, and a 2-round outdoor drive confirmed straight + back is
pixel-perfect and the rotate leg drifts only slightly.

## TL;DR

- **Phase B-3b** (port self-exclusion patch into FAST-LIO-SAM): done.
  Patch lives at `docs/patches/2026-05-30-fastlio-sam-self-exclusion.patch`.
- **Phase B-4** (live verify + buried-map fix): IMU sign-flip republish
  node `imu_sign_flip.py` shipped. grav_align now decides "+Z is up"
  and the map renders the right way round.
- **Outdoor PCD result**: 298 k points, X range 204 m, Y range 123 m,
  Z p5–p95 = 11.5 m, Z median = -0.88 m (≈ ground). Compare to the
  pre-fix run where the median was tens of metres below ground.
- **Branch state**: `m5e/velodyne-self-filter`, 3 commits ahead at the
  time this log was written. This commit is the Phase B closing.

## What broke and how we tracked it

### Symptom 1 — RViz says `Frame [camera_init] does not exist`

FAST-LIO-SAM publishes the map TF as `map`, not `camera_init`. Stock
RViz config from FAST-LIO carries `camera_init` because that's the
upstream HKU-MaRS frame name. Fixed by setting Fixed Frame = `map` in
the FAST-LIO-SAM RViz panel.

### Symptom 2 — `Failed to start FAST-LIO-SAM, no /Odometry topic`

`imu_sign_flip` was launching but the source file lacked the executable
bit. `--symlink-install` symlinks the script through to install/, so the
executable bit must already be set on the source. Fixed with:

```bash
chmod +x src/whill_sensors_bringup/scripts/imu_sign_flip.py
```

(Re-build not needed under symlink-install.)

### Symptom 3 — `incompatible QoS / RELIABILITY_QOS_POLICY` warning

`imu_sign_flip.py` initially used `BEST_EFFORT` for both subscribe and
publish (copy-paste of `velodyne_self_filter.py`'s `sensor_data_qos`).
But the rt_usb_9axisimu_driver publishes `/imu/data_raw` with
`RELIABLE` (rclcpp default), and FAST-LIO-SAM subscribes with `RELIABLE`
as well. Changed `imu_sign_flip.py` to `RELIABLE` end-to-end and the
warnings disappeared.

### Symptom 4 — Buried map

User report on the first 2-round drive:

> rviz上に表示させるマップですがあまり良好ではないかと z軸負の方向に
> 建物や木の情報が出ている気がする

`ros2 topic echo /imu/data_raw --once` on the static chair returned
`linear_acceleration.z ≈ -9.6 m/s²` instead of REP-145's expected
`+9.81`. PCMK-G3X reports the gravity vector directly, not the
"force needed to hold the IMU still" (= -gravity) of REP-145. With the
raw signal, FAST-LIO's `grav_align` decides "+Z is down" and the
reconstructed environment is inverted relative to the world frame.

The fix was a small republish node, `imu_sign_flip.py`, that flips the
sign of the linear acceleration (orientation field is preserved
untouched — FAST-LIO does not use it; `orientation_covariance[0] = -1`
already marks it as unavailable upstream). Then
`fast_lio_sam_velodyne.yaml`'s `imu_topic` was switched from
`/imu/data_raw` to `/imu/data_corrected`.

Live re-check: `ros2 topic echo /imu/data_corrected --once` → `z ≈
+9.58`. Map oriented correctly. User confirmed: 「建物もz軸正側に出てる」.

### Symptom 5 — Rotation drift after the accel fix

A second 2-round drive (this time with the corrected IMU) reported:
"直進してそのままバックする" was clean and the asphalt-step bump was
reflected exactly. But "直進してそのまま旋回" diverged — final pose
landed metres ahead of the start.

Hypothesis: the same PCMK-G3X "device convention" reasoning we applied
to the accel was being applied to the gyro by reflex. But the gyro
might already be in REP-145 (right-handed) — and inverting a correct
sign would make the EKF prediction direction disagree with what
LiDAR-scan registration actually observes during a turn. The covariance
fight would inflate, the pose would drift, and rotation specifically
(not translation) would suffer. That matches the symptom exactly.

Test: set `flip_gyro=False` in `sensors_launch.py`. Re-run. Rotation
behaviour improved markedly — final pose now sits "出発点より少し先" of
the start, no more major drift. The accel-only flip is the holding
configuration.

## What the working IMU still tells us

`linear_acceleration` on the corrected topic, static-state:

  x = +0.06 m/s²   y = +2.68 m/s²   z = +9.58 m/s²

A 2.68 m/s² lateral component on a level chair means the IMU board is
mounted with **~16° forward pitch** relative to base_link
(`asin(2.68 / 9.81) ≈ 15.8°`). grav_align absorbs this for SLAM, but
Nav2 will see chair pose as 15.8° forward-pitched until the
`imu_link → base_link` transform reflects the real mount geometry. See
task #34 in `MEMORY.md` (or wherever the live task list is).

### Follow-up — 5 s static measurement on confirmed-flat floor

Sampled `/imu/data_corrected` for 5 s (500 samples, accel σ ≈ 0.02 m/s²
on each axis — truly static):

| quantity | value | reading |
|---|---|---|
| `|a|` | 9.97 m/s² | accel scale healthy (+1.7 % vs g) |
| `pitch` (rotation around IMU X) | **+16.37°** | nose-down forward tilt |
| `roll` (rotation around IMU Y) | **-0.20°** | laterally level |
| gyro x mean | **-1.143 deg/s** | non-zero on a static IMU → gyro X bias |
| gyro y / z mean | -0.03 / +0.16 deg/s | noise floor |

Roll ≈ 0 on a confirmed-flat floor rules out "chair sits on a slope"
and "y-axis accel bias". The 16.37° is real physical mount tilt of the
IMU board. Therefore when `imu_link` is added to the URDF as part of
Phase A-1 finalization, its origin should carry `rpy="0 0.286 0"`
(0.286 rad ≈ 16.37°). Logged as task #34.

Separately: the **-1.14 deg/s gyro X bias** is independent of the mount
(a static IMU should read zero on every axis regardless of orientation).
FAST-LIO's filter mostly absorbs it via LiDAR feedback, but a long
quasi-static segment could let it leak into pitch. Tracked as a new
follow-up: optional `gyro_bias` param on `imu_sign_flip.py` that
subtracts a constant from each axis before republishing.

## PCD evaluation (2026-05-30 outdoor)

`KEY_FRAMES/global.pcd` after the 2-round drive (corrected IMU,
flip_gyro=False):

| metric | value | comment |
|---|---|---|
| points | 298 120 | binary, fields x/y/z/intensity |
| X range | 204.1 m | east–west extent of the route |
| Y range | 123.1 m | north–south extent |
| Z range (full) | -24.4 → +32.2 m (56.6 m) | outliers (treeline, building tops, registration tails) |
| Z p5 → p95 | -4.8 → +6.7 m (11.5 m) | main body — realistic for an outdoor scene with buildings |
| Z median | -0.88 m | ≈ ground; sub-metre rather than tens of metres below |

For comparison, the previous (pre-fix) buried-map run had a Z median
deep below ground and a Z range over the full 56.6 m centred on -10 m
or worse.

## Files touched by this commit

```
src/whill_localization/config/fast_lio_sam_velodyne.yaml         (new)
src/whill_localization/launch/fast_lio_sam_launch.py             (new)
src/whill_sensors_bringup/scripts/imu_sign_flip.py               (new)
src/whill_sensors_bringup/launch/sensors_launch.py               (+imu_sign_flip Node)
src/whill_sensors_bringup/CMakeLists.txt                         (install imu_sign_flip.py)
docs/patches/2026-05-30-fastlio-sam-self-exclusion.patch         (new)
docs/patches/README.md                                           (+description for new patch)
docs/session-2026-05-30.md                                       (this file)
```

The Phase A URDF leftovers (urdf/, robot_state_publisher_launch.py,
README and package.xml rework) and a stray `ros2global.rviz` at the
repo root are not in this commit — they are pre-existing Phase A
finalization and will land separately.

## Open items going into the next session

1. **URDF `imu_link` pitch correction** — add roll=0, pitch=+0.276 rad
   (15.8°), yaw=0 to `imu_link` in `whill_with_sensors.urdf.xacro` so
   downstream consumers see the chair as level when it is level.
2. **RViz config Best Effort save** — the `Message Filter dropping…
   queue is full` warnings during start-up trace to publisher/RViz
   QoS mismatches. Save the corrected `fast_lio_sam.rviz` with
   `Reliability: Best Effort` on each display.
3. **Long-loop / loop-closure verification** — the 2-round route was
   too short to fire FAST-LIO-SAM's loop closure
   (`historyKeyframeSearchRadius=10`, `historyKeyframeSearchTimeDiff=20`).
   A 50–100 m closed-loop walk around a building will exercise the
   backend correction. Watch `/OdometryGlobal` for the characteristic
   step relative to `/Odometry`.
4. **GNSS integration** — NEO-M8P confirmed on `/dev/ttyACM0`.
   `ublox_dgnss` + `robot_localization`'s `navsat_transform_node` is
   the standard path for fusing as a `map → odom` correction source.

## Reproduce from a fresh shell

```bash
cd ~/whill_lab0_ros2
source install/setup.bash
mkdir -p src/third_party/FAST_LIO_SAM/PCD \
         src/third_party/FAST_LIO_SAM/KEY_FRAMES/scans \
         src/third_party/FAST_LIO_SAM/IMU_POSES
# T1
ros2 launch whill_sensors_bringup sensors_launch.py
# T2 (after T1's IMU activates)
ros2 launch whill_localization fast_lio_sam_launch.py
# T3 (sanity)
ros2 topic echo /imu/data_corrected --once   # z ≈ +9.6 m/s² static
ros2 run tf2_ros tf2_echo map base_link
```

Drive a short out-and-back; the `KEY_FRAMES/global.pcd` is written
automatically on Ctrl+C of T2.

---

## Phase C-1 → C-3 (afternoon): GNSS bring-up

Driver chosen: `ros-humble-ublox-gps` (apt). The newer
`ublox-dgnss`/`ublox-dgnss-node` only target Gen 9 receivers
(ZED-F9 family) — NEO-M8P is Gen 8 and would not work with them.
`ublox-gps` carries an official C94-M8P rover template.

### C-1 — receiver bring-up

Started from `c94_m8p_rover.yaml` and ran into two CFG-* failures in
sequence:

1. `Failed to read the GNSS config` — happened when we asked the
   driver to disable BeiDou while the receiver had it active. The
   constellation reconfiguration triggers a GNSS sub-system restart
   on the receiver and the CFG read-back times out.
2. `Failed to cold reset device after configuring GNSS` (NACK on
   CFG-GNSS 0x06/0x3e) — even after matching all enable bits to the
   receiver's current state (GPS+GLO+BDS+QZSS), the driver still
   declared the BeiDou per-constellation fields "different" and tried
   to rewrite, then the cold-reset that should follow returned NACK.

Resolution: set `config_on_startup: false` in
`whill_localization/config/ublox_m8p_rover.yaml`. The chair's NEO-M8P-2
ships with a working factory/EEPROM configuration; we leave it
alone and just open the port to read. Trade-off captured in the yaml
comments — `dynamic_model`, `nav_rate`, `dgnss_mode` parameters become
inert until we own a clean baseline (e.g. after a u-center factory
reset).

Receiver banner confirmed `MOD=NEO-M8P-2`, `FWVER=HPG 1.40REF`
(High Precision GNSS firmware, RTK-capable), `PROTVER=20.30`,
`ANTSTATUS=OK`. After the `config_on_startup: false` change,
`/fix` appeared at 1 Hz with `frame_id: gnss_link`.

Launch wrapper: `whill_localization/launch/gnss_launch.py` — single
ublox_gps_node spawn, output='both' for first-run visibility, with
an OnProcessExit handler that takes the launch down if the node
dies (USB unplug etc.).

### C-2 — /fix quality over 60 s static

Script: `/tmp/fix_quality_check.py` (not checked in; one-shot
diagnostic). Chair stationary on the asphalt, NEO-M8P antenna
relocated from the under-chair bucket to the rider's footrest for a
better sky view.

| metric | value |
|---|---|
| samples | 54 over 60 s (≈ 1 Hz, light startup loss) |
| status | 54 / 54 in 3D FIX (status.status=0) |
| **horizontal scatter (1σ, observed)** | **r = 2.17 m** (x=1.65, y=1.41) |
| vertical scatter (1σ, observed) | 1.83 m |
| receiver-reported 1σ horiz (start → end) | 20.7 → 17.3 m |

The receiver-reported covariance is consistently ~10× the
actually-observed scatter — typical for u-blox NEO-M8 family in
standalone (non-RTK) mode. The reported number is what
`navsat_transform_node` will see, so the EKF will weight GPS
conservatively. Net result: GPS correction will pull `map → odom`
toward truth slowly but reliably, which is preferable to
overshoot. 2 m of true accuracy is already vastly better than
FAST-LIO outdoor drift (which exceeded 18 % on a 60 s drive in
earlier tests).

### C-3 — gnss_link in URDF

Added to `urdf/whill_with_sensors.urdf.xacro` as a base_link child
with a magenta cylinder visual (so it stands out from imu_link
orange / velodyne red / camera_link black in RViz). Initial offset
`(0.40, 0.00, -0.10)` to match the temporary footrest mounting,
explicitly marked TEMPORARY in the file comments. The antenna is
not mechanically fixed yet; re-measure with a tape before Phase C-6
long-distance drift verification.

`xacro` expansion confirms `xyz="0.4 0.0 -0.1"`; runtime `tf2_echo
base_link gnss_link` deferred to the next session when the full
sensors_launch + gnss_launch + state_estimation chain comes up
together for Phase C-4.

---

## Resume point for next session

Open work, in order:

1. **Phase C-4 — navsat_transform_node integration**.
   - Add a `navsat_transform_node` to
     `whill_localization/launch/state_estimation_launch.py` (or to
     a new `localization_launch.py` umbrella).
   - Inputs: `/fix`, `/imu/data_corrected`, `/odometry/filtered/local`.
   - Output: `/odometry/gps`, `/gps/filtered`.
   - Recommended yaml settings:
     - `use_odometry_yaw: true` — PCMK-G3X's orientation field is
       marked unavailable (`orientation_covariance[0] = -1`) so the
       node can't derive heading from /imu directly. Using
       odometry yaw lets ekf_odom's wheel+gyro integration carry
       the initial heading; the EKF then closes the loop once
       /fix start arriving.
     - `magnetic_declination_radians: 0.0` — we are NOT using a
       magnetic-compass yaw; declination doesn't apply.
     - `broadcast_utm_transform: false` — UTM debug TF not needed.
     - `zero_altitude: true` — matches `two_d_mode: true` on ekf_map.
     - `yaw_offset: 0.0` — let the EKF discover it.
     - `wait_for_datum: false` — accept the first /fix as the
       reference; saves us a manual datum publish.
2. **Phase C-5 — ekf_map `gps0` wiring**. Add `/odometry/gps` as
   `gps0` in `whill_localization/config/ekf_map.yaml` with a
   position-only mask `[true, true, false, false, ..., false]`
   alongside the existing odom0 (ekf_odom local) and odom1
   (FAST-LIO) sources. Don't touch process_noise_covariance —
   GPS reading covariance is already conservative enough.
3. **Phase C-6 — drift correction live test**. 100 m outdoor
   route with a return leg; compare `/OdometryGlobal` (FAST-LIO-SAM
   alone) vs `/odometry/filtered/global` (ekf_map + GPS) as RViz
   Path overlays. Success = the GPS-fused path snaps the return
   leg to within a metre of the starting point even where
   FAST-LIO-SAM would otherwise drift.

Side tasks that can stack onto Phase C without blocking:
- #36 — gyro_bias subtraction in `imu_sign_flip.py` (-1.14 deg/s
  on the IMU X axis, measured 2026-05-30).
- #35 — save fast_lio_sam.rviz with all Display Reliabilities
  set to Best Effort to silence the QoS warnings.

Branch state at session end: `m5e/velodyne-self-filter` at
`94ad310` (Phase A-1 finalization) plus this Phase C-1/C-2/C-3
commit. main merge held off until just before Phase C-6 / GNSS
switchover, per session-end agreement.
