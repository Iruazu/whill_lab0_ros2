# M5-e/Phase A〜C 凍結記録 (2026-06-13)

Language: [日本語](2026-06-13-m5e-frozen.md) | [English](../../en/legacy-findings/2026-06-13-m5e-frozen.md)

## 凍結日付と根拠

2026-06-13、Issue #28 で正式に凍結を執行した。執行の上位根拠は方針文書
[`../plans/2026-06-11-platform-pivot.md`](../plans/2026-06-11-platform-pivot.md) §5
「Claude Code への行動規範 (本方針下での絶対則)」の禁止 #1, #2 と、
§4「マイルストーン再定義」冒頭の凍結宣言である。

§4 (`docs/ja/plans/2026-06-11-platform-pivot.md:134`) 逐語:

> 旧 M5-d / M5-e は凍結し、以下に置き換える。各フェーズの詳細計画は
> 着手時に `pm-orchestrator` が本文書を入力として `docs/plans/` に作成する。

§5 (`docs/ja/plans/2026-06-11-platform-pivot.md:153-155`) 逐語 (禁止 #1, #2):

> 1. `tf_bridge_launch.py` の identity 構成を前提とした新機能の追加 (旧 M5-d の続行を含む)
> 2. FAST-LIO をランタイム localizer として強化する作業
>    (パラメータ再調整はマップ作成品質の改善目的に限り可)

凍結を「執行」する物理操作は次の三点で、本 Issue #28 で実施した:

1. アーカイブタグ `legacy/m5e-phase-abc-2026-06-13` を `origin/m5e/velodyne-self-filter`
   HEAD (`9b5be71`) に annotated tag として付与し、`origin` に push した。
2. main の作業ツリーに残留していた未追跡 Phase C 残骸 (`src/whill_localization/config/navsat_transform.yaml`
   と `src/whill_localization/launch/navsat_transform_launch.py`) を削除した。
   これらは git 追跡対象外だったため `rm` で除去した。
3. 本文書 (凍結記録) を `docs/ja/legacy-findings/` と `docs/en/legacy-findings/`
   に並列で作成した。

## Phase A〜C で何を実装したか

`origin/m5e/velodyne-self-filter` (HEAD = `9b5be71`) に積まれた M5-e 系コミットの
全量を、新しい順に列挙する。コミット範囲は本タグ `legacy/m5e-phase-abc-2026-06-13`
の commit から `git log --grep 'M5-e' --oneline` で再現できる。

| SHA | 件名 | 規模 |
|-----|------|------|
| `9b5be71` | M5-e: tune fast_lio_sam RViz config — Best Effort QoS + dense map | (タグ HEAD) |
| `f43ca4a` | M5-e/Phase C-1 + C-2 + C-3: NEO-M8P bring-up + /fix quality + URDF gnss_link | 4 files, +332 |
| `0785994` | M5-e/Phase 3: chair-self exclusion (cylinder + 2 inner sectors) | — |
| `65e5a2f` | M5-e/Phase B-3 + B-3b + B-4: FAST-LIO-SAM bringup + IMU sign-flip fix | 8 files, +698/-1 |
| `f16e715` | M5-e/Phase B-1,2: add FAST-LIO-SAM (RightTr) to .repos and validate build | — |
| `94ad310` | M5-e/Phase A-1 finalization: URDF + robot_state_publisher + imu_pitch=+16.4° | — |
| `204989e` | M5-e/Phase A: 二段 EKF で Nav2 標準 TF tree に正式移行 | 23 files, +2553/-62 |

Phase 単位での意図と成果物:

- **Phase A (二段 EKF + Nav2 標準 TF tree)**: `ekf_odom` と `ekf_map` の 2 つの
  robot_localization インスタンスを並列で動かし、`map -> odom -> base_link`
  の REP-105 階層を組んだ。`/whill/odom` を含む odom インスタンスと、
  FAST-LIO 出力を融合する map インスタンスを分離するアプローチ。
- **Phase B (FAST-LIO-SAM bringup)**: ループクロージャを持つ FAST-LIO-SAM
  (RightTr fork) を `.repos` 経由で取り込み、IMU sign-flip 修正を含む
  bringup launch を整備した。
- **Phase C (NEO-M8P GNSS)**: GNSS フィックス品質判定 + URDF への `gnss_link`
  追加 + navsat_transform_node 経由の絶対位置補正経路を試作した。

主要な成果物 (個別ファイル、すべて `9b5be71` の作業ツリーに存在):

- `src/whill_odometry/` — Phase A で新規追加した独立パッケージ。
  C++ ノードは 276 行規模。`/whill/odom` を生成する車輪 odometry の
  独立化を試みた。
- `src/whill_localization/config/ekf_odom.yaml`, `ekf_map.yaml` — 二段 EKF 設定。
- `src/whill_localization/launch/state_estimation_launch.py` — 二段 EKF を
  並列起動する launch。
- `src/whill_localization/launch/fast_lio_sam_launch.py`,
  `gnss_launch.py`, `navsat_transform_launch.py` — Phase B/C の bringup。
- `src/whill_localization/config/fast_lio_sam_velodyne.yaml`,
  `ublox_m8p_rover.yaml`, `navsat_transform.yaml` — 上記 launch の設定。

## なぜ凍結したか

方針文書 §0「背景」(`docs/ja/plans/2026-06-11-platform-pivot.md:15-23`) 逐語:

> 2026-06 の技術調査 (SLAM / localization 手法、つくばチャレンジ完走チーム構成、
> ライセンス、計算資源) と現実装の診断により、以下が確定した:
>
> - 現在の M5-a TF ブリッジ (`map -> camera_init` identity 固定) は
>   「短時間・有人・マッピング開始地点からの起動」というデモ条件でのみ成立する近似である
> - run3 (歩行者横断で FAST-LIO 発散) が示す通り、この近似は
>   「人の往来がある環境での運用」という本来の目標と原理的に両立しない
> - この近似の上に M5-d (goal-following) / M5-e (tuning) を積み増す現在の軌道が
>   「間違った方向」の正体であり、機能追加を止めて土台を差し替える必要がある

方針文書 §2「現状の問題 (診断要約)」P1〜P5 (`docs/ja/plans/2026-06-11-platform-pivot.md:51-57`)
逐語:

> | ID | 問題 | 根拠 | 矛盾する要件 |
> |----|------|------|------------|
> | P1 | 運用時の自己位置に補正経路がない。`map -> camera_init` identity 固定で、FAST-LIO のドリフト (実測 18%/60s) がそのまま map 座標の誤差になる | `tf_bridge_launch.py` | R2, R3 |
> | P2 | 初期位置合わせ機構がない。camera_init = 起動位置のため、マッピング開始地点・同方位で起動しないと最初からズレる | `tf_bridge_launch.py`, `pcd_to_occupancy_grid.py` の origin 前提 | R1 |
> | P3 | 発散を検知も回復もしない。歩行者 1 人で発散し得る (run3 実測) のに TF は出続け、Nav2 は走行を継続する | `whill_localization/README.md` run3 | R4 |
> | P4 | odom フレーム不在・車輪オドメトリ未使用。補正導入時のジャンプ緩衝材がなく、LiDAR 縮退時のバックアップもない | `nav2_params.yaml` コメント、CLAUDE.md 既知課題 | R3, R4 |
> | P5 | 地図品質の問題が安全機能を連鎖停止させている。ゴースト障害物 -> `use_collision_detection: false`、QoS 不一致 -> obstacle layer なし。運用中の歩行者が costmap に一切映らない | `nav2_params.yaml` コメント | R5 |

Phase A の二段 EKF が P1 と P3 を構造的に解決できない理由を補記する。
これは「Phase A をもう少し詰めれば M4-R 相当になるのではないか」という
延命論を明示的に却下するための記録である:

- Phase A は `map -> odom -> base_link` の TF 階層を整えたが、`map -> odom` を
  動かす補正源 (scan-to-map マッチング) を持っていない。Phase A 内では
  FAST-LIO 出力を `ekf_map` に流し込むことで近似していたが、これは方針 §3.2
  で「ランタイム localizer 強化禁止」と定めた経路であり、P1 (補正経路の不在)
  を構造的には解決していない。
- Phase A は発散検知ロジック・フェイルセーフを一切持たない。EKF の共分散が
  膨らんでも `cmd_vel` は遮断されず、Nav2 は走行を継続する。これは P3 の
  「TF は出続け、Nav2 は走行を継続する」状態そのものを Phase A が再現している。
- 上記 2 点は Phase A の追加実装で「足し算的に」解決できる類の問題ではない。
  M6-R で別アーキテクチャ (scan-to-map localizer + フェイルセーフノード) を
  ゼロから組む必要がある。Phase A は「TF 階層の形」だけは正しく、
  「中身の責務分担」が方針と噛み合っていない。

方針 §5「Claude Code への行動規範」(`docs/ja/plans/2026-06-11-platform-pivot.md:149-159`)
で、上記の判断は「絶対則」として明文化された。`tf_bridge_launch.py` の
identity 構成への新機能追加 (禁止 #1) と FAST-LIO のランタイム localizer 強化
(禁止 #2) はいずれも禁止対象であり、Phase A〜C の延長線は当該禁止に抵触する。

## アーカイブタグへの参照

- タグ名: `legacy/m5e-phase-abc-2026-06-13`
- 指す commit: `9b5be714517320102a46d4b19946f2248cb4ecd2`
  (`origin/m5e/velodyne-self-filter` HEAD)
- GitHub リンク: <https://github.com/Iruazu/whill_lab0_ros2/releases/tag/legacy/m5e-phase-abc-2026-06-13>
- ローカル参照方法:

  ```bash
  git fetch --tags origin
  git checkout legacy/m5e-phase-abc-2026-06-13
  git log --grep 'M5-e/Phase' --oneline
  ```

  上記 `git log --grep` は本タグから到達可能な全 Phase コミットを一括表示する。

## 何が残っているか

凍結後も、なぜ Phase A〜C が選ばれなかったかを将来再検討するための参照経路は
以下のとおり保持される:

- `origin/m5e/velodyne-self-filter` ブランチ。本 Issue では削除しない。
  Phase A〜C 系統の HEAD を branch ref としても保持し、IDE の branch picker
  からも到達可能にする。
- `legacy/m5e-phase-abc-2026-06-13` タグ。本 Issue で付与した annotated tag。
  ブランチが将来 force-push やリネームで動いた場合でも、commit への参照は
  タグ側で固定される。
- `git log --grep 'M5-e/Phase' --all --oneline` で、ブランチ・タグを横断して
  M5-e 系統のコミット履歴を一括取得できる。

## 関連

- 開発方針: [`../plans/2026-06-11-platform-pivot.md`](../plans/2026-06-11-platform-pivot.md)
  (特に §0「背景」、§2「現状の問題」、§5「Claude Code への行動規範」)
- 関連 Issue: #28 (本 Issue)、Issue #16〜#19 (docs 二言語化系)、
  Issue #20〜#23 (並行 chore 系)
- 後続: M4-R 実行系 Issue (2026-06-13 確定予定)
